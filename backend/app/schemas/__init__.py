"""
Pydantic Schemas Package

This package contains all request/response schemas for API endpoints.
Currently schemas are defined inline in API files, but can be moved here
for better organization.

Example usage:
    from app.schemas.auth import LoginRequest, LoginResponse
    from app.schemas.session import SessionCreate, SessionResponse
"""

# TODO: Move schemas from api/ files here for better organization
# Example structure:
# - auth.py: Authentication related schemas
# - session.py: Session related schemas
# - employee.py: Employee related schemas
# - abnormality.py: Abnormality related schemas
# - report.py: Report related schemas

__all__ = []